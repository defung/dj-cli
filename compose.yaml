---
services:
  dj-cli:
    build:
      context: .
      network: host
    image: registry.dj-home.org/dj-cli:latest
    container_name: dj-cli
    # Default shows help - override with docker compose run
    command: ["--help"]
