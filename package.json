{
  "name": "dj-cli",
  "version": "0.0.1",
  "description": "A set of scripts to run on server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx ts-node src/index.ts",
    "clean": "rm -rf dist",
    "build:js": "tsc",
    "build:bundle": "node build-bundle.js",
    "build:deps": "node build-deps.js",
    "build:sea": "node build-sea.js",
    "build:docker": "npm run clean && npm run build:js && npm run build:bundle",
    "build:all": "npm run clean && npm run build:js && npm run build:bundle && npm run build:sea"
  },
  "cliFileName": "dj",
  "keywords": [],
  "author": "defung",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@actual-app/api": "^25.10.0",
    "@commander-js/extra-typings": "^14.0.0",
    "@types/node": "^22.18.7",
    "commander": "^14.0.1",
    "esbuild": "^0.25.10",
    "subtitle": "^4.2.2-alpha.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@actual-app/api": "^25.9.0",
    "better-sqlite3": "^12.4.1"
  }
}
